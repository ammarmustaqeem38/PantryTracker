# PantryTracker
A Python desktop app to help keep track of food since I found myself constantly accidentally expired goods. The date format is "DDMMYY". I also added a notification system that flags items expiring within three weeks and used JSON so the inventory actually saves when you close the app.
